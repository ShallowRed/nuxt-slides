/**
 * Build-time configuration (audit §7 P2 #10) — a single typed reader for the
 * standalone-bundle env toggles, shared by `nuxt.config.ts` and the frozen-bundle
 * Nitro plugin.
 */
export * from './bundle'
