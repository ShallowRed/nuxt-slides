/**
 * Content sourcing (audit §5.7 / Axe F) — a first-class, declared content source
 * that replaces the implicit submodule + token + ad-hoc-script tangle with one
 * validated config object and a pure fetch-policy decision.
 */
export * from './source-config'
